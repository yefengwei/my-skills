# 📚 课程笔记整理方法论 · 实战沉淀版 v3.0

> 适用场景：把一门课程（视频 + 字幕 + 课件）高效整理成**图文并茂、扫一眼就能用**的知识手册。
> 本文档由「v1/v2 方法论 + 多次真实整理实战（PM 阶段 / 考研语法 / 生命管理 / 长难句精讲）」沉淀而来，重点补强了 **关键帧"有意义"标准** 与 **v2.0 备忘快贴**。
> 已有产出示例：`内容项目-知识手册.md`、`考研英语语法长难句-知识手册.md`、`生命管理-知识手册.md` 及其同名 `关键帧/`。

---

## 🎯 一句话概括

**从视频字幕提取内容主干（若无可用 PDF）→ 从关键帧抽取"内容完整、无遮挡、能看清知识"的课件画面做配图 → 每个知识点穿插多张 → 用 Mermaid 绘制逻辑图 → 每章末贴一张「备忘快贴」，最终合成一份「图文并茂、随时可扫」的 Markdown 知识手册。**

> 与旧版最大区别：**关键帧不再"随便抽一张"**，而是"**挑有意义的那张/那几张**"；手册不只"看过记得住"，更要"**扫一眼就能用**"。

---

## 🧩 产出物清单

| 产物 | 说明 |
|------|------|
| `知识手册.md` | 最终交付。每章 = 正文（表格/配图/Mermaid）+ 章节总结 + **📌 备忘快贴** |
| `关键帧/` | 精选的**有意义**课件画面（jpg） |
| `关键帧/映射清单.txt` | 帧文件 ↔ 视频/知识点的对应关系 |
| （可选）`提取中间文本/` | 字幕、PDF 抽取的临时文本，用于溯源 |

---

## 🛠️ 前置工具

| 工具 | 用途 |
|------|------|
| **ffmpeg / ffprobe** | 提取视频时长、抽取关键帧 |
| **Python 3** | 批量处理脚本（字幕清洗、PDF 抽取、帧搬移） |
| **pypdf** | 提取课件 PDF 文本（`pip install pypdf`） |
| **@mermaid-js/mermaid-cli** | 校验 Mermaid 语法（`npm i -g @mermaid-js/mermaid-cli`，命令 `mmdc`） |
| **视觉审阅**（image 工具 / 作子代理） | 对候选帧批量打分筛选，**决定"这张有没有意义"** |

> ⚠️ Mermaid 语法校验关键：**所有 Mermaid 块必须实际渲染通过**，确保无 `Syntax error in text`。

---

## 📋 六步工作流

### 第一步：梳理课程结构

1. 扫描源目录，列出所有章节、每个章节的视频数与字幕数。
2. 读取课程 `README` / 目录页 / 文件命名，建立**章节目录总览表**（章节名、一句话核心）。
3. 确定本次整理的「学习主线」和「项目背景」。

> 💡 这一步帮你建立全局框架，后面每个知识点都能挂靠到正确的位置。

### 第二步：判断内容源，确定"主干来源"

**先做源探测，决定用哪种作为内容主干**（这是实战中最关键的判断）：

| 源的情况 | 处理方式 | 内容主干 |
|---------|---------|----------|
| 有**可用课件 PDF** | 用 pypdf 抽取文本 | PDF（结构化骨架）+ 字幕（逻辑） |
| 只有 **视频+字幕** | 直接用字幕（逐字稿） | 字幕 |
| 字幕是**机翻同音乱码**（如"主谓宾→主为兵"） | 字幕只作逻辑辅助，**以课件关键帧画面为准** | 关键帧画面文字 |
| 有 PDF 但抽取**乱码**（自定义字体编码） | pypdf 抽不出 → 降级用字幕/关键帧 | 字幕/关键帧 |

> 💡 **关键子步**：pypdf 抽出来是 `䧴⾓ⵖ暵㹁涸` 这类乱码时，说明 PDF 用了自定义字体编码，**别硬用**，立即降级。
> **字幕乱码时**：该语言课程的字幕大多是同音误识别，**不能逐字誊抄**，但能提供"老师讲了哪几个点、用什么例子"的逻辑，用来组织章节、补全例子，正文知识点则从**清晰的关键帧画面**读出来。

### 第三步：逐章精读 + 撰写手册正文

阅读每章的字幕与课件，提炼知识点，按以下结构撰写：

```
# 一、第01章标题
> 本章目标一句话

## 1.1 小节标题
- 核心定义 / 表格 / 要点
> 💡 一句话记住（通俗理解）

...

## 1.N 章节总结            ← 回顾全章：关键帧 + Mermaid 总览图 + 要点列表
### 📌 备忘快贴           ← 压成"扫一眼就能用"的速记卡（规范见后）
```

**撰写要点**：
1. 用**表格**归纳对比（概念、优劣、适用场景）。
2. 用 `> 💡`、`> ⚠️` 标注理解口诀、易错点。
3. 每条知识点「从浅入深」：先给定义 → 再给例子 → 再给要点。
4. 标题、知识点**与视频/字幕标题一一对应**，方便追溯。
5. **每章写完后立即贴「备忘快贴」**（趁内容还热，从刚提炼的要点中筛"最能上手/最该记住"的），不要等整本写完再补。

### 第四步：抽取并精选"有意义"关键帧（本方法核心）

#### 4.1 先密集抽候选帧（打底）

不要只用 45% 一个点——用一个**更密的时间网格**抽候选帧，方便找到"主讲人没挡住内容"的时刻：

```python
# 每课抽 ~11~30 个时间点（百分比从 ~4% 到 ~96%），得到一批"候选帧"
pcts = [0.04 + i*(0.92/(N-1)) for i in range(N)]   # N=11~30
# 输出到 关键帧_候选/，命名 chap<课号两位>_<序号>.jpg
```

```bash
ffmpeg -y -v error -ss <秒数> -i <视频.mp4> -frames:v 1 -q:v 2 <输出.jpg>
```

> 💡 密度建议：**每课 11 张起步**（覆盖全片），若视频长、主讲人常挡住讲台，则加到 **30 张**，提高找到无遮挡帧的概率。候选帧单独放一个目录，别污染正式目录。

#### 4.2 视觉审阅打分（决定"有没有意义"）

用 image 工具逐张审阅候选帧（量大时可派**子代理**，一次传 8–12 张，务必全部覆盖），按下面标准判"入选/剔除"。

**✅ 入选必要条件（缺一不可）**：
1. 画面主体是**课件/板书/真题的完整展示**，含具体知识（公式、表格、例句、中英对照、成分标注、真题长难句等）。
2. 知识内容**完整可见**——不被主讲人的头/手/身体遮挡关键文字，不是"被截断、被手挡住"。
3. **文字清晰可读**——不模糊、不过小、不被水印/杂物大面积覆盖。
4. 与本章知识点**密切相关**。

**❌ 坚决剔除**：
- 主讲人特写占大头 / 黑屏 / 纯人物。
- 文字被手/头/身体**遮挡**、内容**被截断不全**。
- 画面杂乱模糊、纯片头片尾 / 纯标题页。

> ⚠️ **最容易踩的坑**：一张图"虽然出现了语法内容，但老师的手/身体挡住了句中关键部分"——**这类一定要剔**，宁可少要，也不要"看图猜不全内容"。
> ⚠️ **规避方法**：挑主讲人**站到旁边、完整露出课件**的那一帧；这类画面在密网格里一定能找到。

#### 4.3 每个知识点/小节穿插多张

不要"一节只插一张"。同一知识点的不同课件画面（公式页、例句页、真题分析页、手写标注页）**并排放 2–4 张**，读者可逐帧对照细看、互相补充。

```markdown
![主谓宾 vs 主系表对比 · 关键帧](关键帧/chap01_13.jpg)

![系动词分类 · 关键帧](关键帧/chap01_16.jpg)

![主谓宾宾补结构 · 关键帧](关键帧/chap01_25.jpg)
```

> 💡 一条经验：**每个知识点/小节 2–4 张**，比"每课 1 张"的信息完整度和可读性高很多。

#### 4.4 抽帧后必须抽查

选完立即用视觉再抽查一遍，确认：① 内容完整无遮挡；② 是课件/真题画面，非黑屏/人像；③ 图片未误插进 mermaid 代码块。

#### 4.5 精选帧归位 + 写映射清单

把入选的"有意义"帧从候选目录**拷贝/仅保留**到正式 `关键帧/`，并写 `关键帧/映射清单.txt`：

```
第1章 简单句（课01–08）
课程01 基本结构
- chap01_13.jpg —— 主谓宾 vs 主系表结构对比及正误例句
- chap01_16.jpg —— 系动词分类（be 与感官动词）
- chap01_25.jpg —— 主谓宾宾补结构公式与标注
```

> 💡 正式目录**只保留精选帧**，未入选的候选帧留在 `关键帧_候选/`（可删可留），避免"正式目录里混着一堆无意义/被遮挡帧"。

### 第五步：绘制并校验 Mermaid 图

对涉及**业务流程 / 功能逻辑 / 结构层级**的知识点，绘制 UML 图：

| 场景 | 推荐图型 |
|------|---------|
| 业务流转、判断分支 | `flowchart TD/LR` |
| 多角色协作 | `flowchart` + `subgraph`（泳道感） |
| 状态流转 | `stateDiagram-v2` |
| 实体关系、多对多 | `classDiagram` |
| 结构层级 | `flowchart` / `graph` |

**兼容性规范**（兼容新旧版本、杜绝语法错误）：
1. 统一用 `flowchart`、`stateDiagram-v2`、`classDiagram` 等稳定语法。
2. 节点 ID 只用字母/数字/下划线，中文放 `[ ]` 内。
3. 用 `<br/>` 换行，避免特殊字符裸用（尤其 `( ) : + /` 等不要裸放在 `[ ]` 内的中文/英文之间）。
4. 连接符规范：`-->`、`-- 是 -->`、`-.->`。

> ⚠️ **实战踩坑**：节点标签里放 `:、+、( )、/` 会报 `Syntax error`。改成 `[双宾结构] --> |向某人| C[to 结构]` 这种"特殊符号放边标签、不放节点标签"的方式即可通过。

**质量校验**（必做）：

```bash
# 把手册中所有 ```mermaid 块提取成独立 .mmd 文件
# 逐一用 mmdc 渲染：
mmdc -i block_N.mmd -o block_N.svg   # 无报错即通过；有报错需修正
# 若 mmdc 报 "Could not find chrome-headless-shell"，可用已装 Edge 做 puppeteer 配置：
#   puppeteer.json = { "executablePath": "<msedge.exe 路径>", "args": ["--no-sandbox","--disable-setuid-sandbox"] }
#   mmdc -p puppeteer.json -i block_N.mmd -o block_N.svg
```

> ✅ 校验清单：① 所有块渲染成功；② 关键帧图片未被误插入 mermaid 代码块内；③ 图片引用路径有效。

---

## 📌 备忘快贴规范（v2.0 核心）

### 它是谁

**备忘快贴** = 贴在每章末尾的"速记便签"，把整章浓缩成 **4 个槽位**，让人**扫一眼（≤ 10 秒）就能直接上手 / 唤醒记忆**。

与「章节总结」分工：

| | 章节总结 | 备忘快贴 |
|---|---|---|
| 目的 | **回顾全章结构**（我学了什么） | **随时调用**（过两天怎么用/怎么记） |
| 形态 | 图 + Mermaid + 要点列表，篇幅可长 | 4 行引用块，极短 |
| 阅读时机 | 学完一章当时 | 之后任何时刻回扫 / 复习 |

### 固定模板（4 槽位，勿增删）

```markdown
### 📌 备忘快贴

> **💬 一句话** ｜ <本章核心，≤ 25 字，能当口头回答讲给别人>
>
> **⚡ 拿来就用** ｜ <1~3 条立即可执行的动作/命令/步骤，动词开头>
>
> **⚠️ 别踩的坑** ｜ <1~2 条本章最易错 / 最反直觉的点>
>
> **🧠 记忆锚** ｜ <口诀 / 类比 / 钩子，帮长期记忆>
```

### 写作五原则

1. **独立成文**：遮住正文只看快贴，仍能回答"这章讲啥、怎么用、坑在哪"。
2. **只提炼、不新增**：快贴每条都必须在正文里出现过，是对正文的"剪辑"。
3. **动词开头、一行说完**：密度优先，宁少勿水，单槽超 2 行必砍。
4. **术语翻译成"人话"**：命令/参数可复制执行；概念类给类比。
5. **全章唯一**：挑全章最能上手、最值得记的那几点，不是逐小节堆叠。

### 快速自检（贴完默念 3 问）

1. 遮住正文，快贴能独立读懂吗？
2. 有没有哪条是正文里找不到出处的？（有 → 删）
3. 10 秒扫完，能立刻说出"这章我要做什么/避开什么"吗？（不能 → 砍到能）

---

## ✅ 最终质检清单（交付前逐项核对）

- [ ] 手册结构完整：一级标题（章）+ 二级标题（小节）齐全
- [ ] **每章末尾都有 📌 备忘快贴**，套用 4 槽位模板
- [ ] 快贴内容在正文可溯源（无新增知识点），遮住正文可独立读懂
- [ ] **关键帧均为"有意义"帧**：内容完整、无遮挡/截断、文字清晰、与知识点相关
- [ ] **每个知识点/小节穿插多张图**（2–4 张），图片路径有效
- [ ] 所有 Mermaid 块 `mmdc` 渲染通过，无 `Syntax error in text`
- [ ] 无图片误插入 mermaid 代码块
- [ ] 文件归位到目标目录（源课程目录下），相对路径正确
- [ ] 表头说明、图注完整，阅读友好

---

## 📁 推荐目录结构（复用模板）

```
<课程阶段目录>/
├── 知识手册.md              # 最终交付（每章含章节总结 + 备忘快贴；知识点穿插多图）
├── 关键帧/                 # 精选的有意义关键帧（jpg + 映射清单.txt）
├── 关键帧_候选/            # 密集抽帧的候选帧（可删可留，供日后重选）
├── 源章节目录/              # 原有视频、字幕、课件（不动）
└── .cowork-temp/           # 中间产物（字幕、PDF抽取、脚本，可随时清理）
```

---

## 🧰 可复用脚本清单（建议沉淀到 `笔记整理脚本/`）

| 脚本 | 功能 |
|------|------|
| `01_提取字幕与PDF文本.py` | 清洗 SRT 字幕、用 pypdf 抽 PDF，按章存为文本 |
| `02_抽取候选关键帧.py` | ffprobe 取时长，按**密网格**（N 点）抽候选帧到 `关键帧_候选/` |
| `03_精选帧归位.py` | 按"入选清单"把有意义帧从候选目录拷贝/仅保留到 `关键帧/` |
| `04_校验mermaid语法.py` | 提取手册所有 mermaid 块并逐一渲染校验（可用 Edge 配置） |
| `05_校验图片路径.py` | 扫描手册所有 `关键帧/xxx.jpg` 引用，校验文件存在、无缺失 |

> 用法：`python 04_校验mermaid语法.py <知识手册.md>`（依赖 npx + @mermaid-js/mermaid-cli）。

---

## 🔁 复用提示

1. **换一套课程**：复制本流程，替换「源目录」与「章节目录」，其余步骤通用。
2. **换强调重点**：重内容 → 每个知识点多插帧；重逻辑 → 多画 Mermaid 图；重实操 → 快贴「⚡ 拿来就用」「⚠️ 别踩的坑」两槽写满写实。
3. **自动化**：第二步、第四步、第五步的校验脚本可复跑，节省时间。
4. **回扫复习**：手册完成后，只翻每章 📌 快贴即可完成一轮快速巩固；卡壳处再回正文细看——这就是"扫一眼就能用"。

---

> ⚠️ 本方法为通用流程模板，实际执行时请根据内容特点灵活调整「抽帧密度」「图表数量」「快贴槽位详略」。最关键的一条实战心法：**关键帧要"有意义"——内容完整、无遮挡、每个知识点多张，宁可少要也不要"看图猜不全"。**
