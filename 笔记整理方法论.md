# 📚 课程笔记整理方法论（可复用工作流）

> 适用场景：把一门课程（视频 + 字幕 + 课件）高效整理成**图文并茂、可随时温习**的知识手册。
> 本方法已用于「02阶段：内容项目」整理，产出：[内容项目-知识手册.md](./内容项目-知识手册.md) 及 [内容项目-关键帧](./内容项目-关键帧)。

---

## 🎯 一句话概括

**从视频字幕提取内容主干 + 从课件 PDF 提取结构化要点 + 从视频抽关键帧做配图 + 用 Mermaid 绘制业务/逻辑图，最终合成一份「图文并茂」的 Markdown 知识手册。**

---

## 🧩 产出物清单

| 产物 | 说明 |
|------|------|
| `知识手册.md` | 最终交付，含正文、表格、关键帧配图、Mermaid 图 |
| `关键帧/` | 从各视频抽取的代表性课件画面（jpg） |
| `关键帧/映射清单.txt` | 帧文件 ↔ 视频/知识点的对应关系 |
| （可选）`提取中间文本/` | 字幕、PDF 抽取的临时文本，用于溯源 |

---

## 🛠️ 前置工具

| 工具 | 用途 |
|------|------|
| **ffmpeg / ffprobe** | 提取视频时长、抽取关键帧 |
| **Python 3** | 批量处理脚本（字幕清洗、PDF 抽取、帧插入） |
| **pypdf** | 提取课件 PDF 文本（`pip install pypdf`） |
| **@mermaid-js/mermaid-cli** | 校验 Mermaid 语法（`npm i -g @mermaid-js/mermaid-cli`，命令 `mmdc`） |

> ⚠️ Mermaid 语法校验关键：**所有 Mermaid 块必须实际渲染通过**，确保无 `Syntax error in text`。

## 🧰 配套脚本（可复用，位于本目录 `笔记整理脚本/`）

| 脚本 | 功能 |
|------|------|
| `01_提取字幕与PDF文本.py` | 清洗 SRT 字幕、用 pypdf 抽取课件 PDF，按章存为文本 |
| `02_抽取视频关键帧.py` | 探测视频时长，取 45% 时间点抽帧，命名 `chapX_Y.jpg` |
| `03_插入关键帧到手册.py` | 按 `## X.Y` 二级标题自动插入对应关键帧配图 |
| `04_校验mermaid语法.py` | 提取手册所有 mermaid 块并逐一渲染校验，防止语法错误 |

> 用法：`python 04_校验mermaid语法.py <知识手册.md>`（依赖 npx + @mermaid-js/mermaid-cli）。

---

## 📋 五步工作流

### 第一步：梳理课程结构

1. 扫描源目录，列出所有章节、每个章节的视频数与字幕数。
2. 读取课程 `README` / 目录页，建立**章节目录总览表**（章节名、一句话核心）。
3. 确定本次整理的「学习主线」和「项目背景」（如内容项目的主线是"专注教育公司做内容平台"）。

> 💡 这一步帮你建立全局框架，后面每个知识点都能挂靠到正确的位置。

---

### 第二步：提取内容主干（字幕 + 课件）

用 Python 脚本批量处理：

```python
# 1. 清洗 SRT 字幕：去掉序号、时间戳，只保留文本
# 2. 用 pypdf 抽取各章课件 PDF 文本
```

- **字幕**：教学内容「逐字稿」，最完整。
- **课件 PDF**：结构化要点（定义、表格、结论），最精炼。
- **两者互补**：字幕补全逻辑，PDF 提炼骨架。

> 💡 建议把提取结果按章节存成 `sub_chXX.txt`、`pdf_XX.txt`，方便逐章精读。

---

### 第三步：逐章精读 + 撰写手册正文

阅读每章的字幕与课件，提炼知识点，按以下结构撰写：

```
# 一、第01章标题
> 本章目标一句话

## 1.1 小节标题
- 核心定义 / 表格 / 要点
> 💡 一句话记住（通俗理解）
```

**撰写要点**：
1. 用**表格**归纳对比（概念、优劣、适用场景）。
2. 用 `> 💡`、`> ⚠️` 标注理解口诀、易错点。
3. 每条知识点「从浅入深」：先给定义 → 再给例子 → 再给要点。
4. 所有标题、知识点**与视频/字幕标题一一对应**，方便追溯。

---

### 第四步：抽取关键帧做配图

从每个视频抽 1 张代表性画面（课件内容展示处）：

```bash
# 先在 Python 中用 ffprobe 取视频时长
# 再取 45% 时间点（课件主体展开处）抽帧
ffmpeg -y -v error -ss <秒数> -i <视频.mp4> -frames:v 1 -q:v 3 <输出.jpg>
```

- **命名规范**：`chap<章号两位>_<视频序号>.jpg`（如 `chap05_7.jpg`）。
- **插入位置**：自动匹配到手册 `## X.Y` 二级标题下。
- **图注**：`![<标题> · 关键帧](相对路径/chapX_Y.jpg)`。

> ⚠️ 抽帧后**必须抽查**：确认是清晰课件画面，非黑屏 / 字幕残留。

---

### 第五步：绘制并校验 Mermaid 图

对涉及**业务流程 / 功能逻辑 / 结构层级**的知识点，绘制 UML 图：

| 场景 | 推荐图型 |
|------|---------|
| 业务流转、判断分支 | `flowchart TD/LR` |
| 多角色协作 | `flowchart` + `subgraph`（泳道感） |
| 状态流转 | `stateDiagram-v2` |
| 实体关系、多对多 | `classDiagram` |
| 结构层级 | `flowchart` / `graph` |

**兼容性规范**（兼容新旧版本、杜绝语法错误）：
1. 统一用 `flowchart`、`stateDiagram-v2`、`classDiagram` 等稳定语法。
2. 节点 ID 只用字母/数字/下划线，中文放 `[ ]` 内。
3. 用 `<br/>` 换行，避免特殊字符裸用。
4. 每条连接符规范：`-->`、`-- 是 -->`、`-.->`。

**质量校验**（必做）：

```bash
# 把手册中所有 ```mermaid 块提取成独立 .mmd 文件
# 逐一用 mmdc 渲染：
mmdc -i block_N.mmd -o block_N.svg
# 无报错即通过；有报错需修正
```

> ✅ 校验清单：① 所有块渲染成功；② 关键帧图片未被误插入 mermaid 代码块内；③ 图片引用路径有效。

---

## ✅ 最终质检清单（交付前逐项核对）

- [ ] 手册结构完整：一级标题（章）+ 二级标题（小节）齐全
- [ ] 关键帧配图数量与知识点对齐，图片路径有效
- [ ] 所有 Mermaid 块 `mmdc` 渲染通过，无 `Syntax error in text`
- [ ] 无图片误插入 mermaid 代码块
- [ ] 文件归位到目标目录（源课程目录下），相对路径正确
- [ ] 表头说明、图注完整，阅读友好

---

## 📁 推荐目录结构（复用模板）

```
<课程阶段目录>/
├── 知识手册.md              # 最终交付
├── 关键帧/                 # 视频关键帧（jpg + 映射清单.txt）
├── 源章节目录/              # 原有视频、字幕、课件（不动）
└── .cowork-temp/           # 中间产物（字幕、PDF抽取、脚本，可随时清理）
```

---

## 🔁 复用提示

1. **换一套课程**：复制本流程，替换「源目录」与「章节目录」，其余步骤通用。
2. **换强调重点**：若重内容 → 多抽取关键帧；若重逻辑 → 多画 Mermaid 图。
3. **自动化**：可把第二步、第四步、第五步的校验写成脚本复跑，节省时间。

---

> ⚠️ 本方法为通用工作流模板，实际执行时请根据内容特点灵活调整「抽帧密度」「图表数量」。
